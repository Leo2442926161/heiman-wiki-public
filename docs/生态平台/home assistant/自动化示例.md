---
sidebar_position: 2
---

# 自动化示例

## 烟雾报警触发全屋亮灯

```yaml
alias: "烟雾报警 - 全屋亮灯并通知"
triggers:
  - trigger: state
    entity_id: binary_sensor.heiman_smoke_alarm
    to: "on"
actions:
  - action: light.turn_on
    target:
      entity_id: all
  - action: notify.mobile_app_phone
    data:
      message: "⚠️ 烟雾报警器触发！请立即检查！"
      title: "烟雾报警"
```

## CO 报警联动

```yaml
alias: "CO 报警 - 关闭燃气并开窗"
triggers:
  - trigger: state
    entity_id: binary_sensor.heiman_co_alarm
    to: "on"
actions:
  - action: switch.turn_off
    entity_id: switch.gas_fireplace
  - action: cover.open
    entity_id: cover.living_room_window
  - action: notify.persistent_notification
    data:
      message: "一氧化碳报警器触发，已关闭燃气阀门并打开窗户"
```

## 水浸探测器联动

```yaml
alias: "水浸检测 - 关闭水阀"
triggers:
  - trigger: state
    entity_id: binary_sensor.heiman_water_leak
    to: "on"
actions:
  - action: switch.turn_off
    entity_id: switch.main_water_valve
  - action: notify.mobile_app_phone
    data:
      message: "💧 检测到漏水！已关闭总水阀！"
```

## 离家布防 - 人体感应触发警报

```yaml
alias: "离家模式 - 人体感应报警"
triggers:
  - trigger: state
    entity_id: binary_sensor.heiman_motion_sensor
    to: "on"
conditions:
  - condition: state
    entity_id: alarm_control_panel.home_alarm
    state: armed_away
actions:
  - action: alarm_control_panel.trigger
    entity_id: alarm_control_panel.home_alarm
```
